server:
  port: 5000
  servlet:
    encoding:
      charset: utf-8
      force: true

spring:
  datasource:
    url: jdbc:mariadb://rds-bankapp.crqatvmsvhw1.ap-northeast-2.rds.amazonaws.com:3306/bank
    driver-class-name: org.mariadb.jdbc.Driver
    username: admin
    password: admin1234!
  config:
    activate:
      on-profile: prod
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      '[hibernate.default_batch_fetch_size]': 100

logging:
  level:
    '[shop.mtcoding.bank]': INFO